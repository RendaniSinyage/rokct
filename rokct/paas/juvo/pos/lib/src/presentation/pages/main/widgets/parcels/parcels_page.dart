import 'package:flutter/material.dart';

class ParcelsPage extends StatelessWidget {
  const ParcelsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Parcels Page - To be implemented"),
      ),
    );
  }
}